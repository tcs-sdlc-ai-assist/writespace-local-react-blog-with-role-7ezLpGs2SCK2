# .env.example
# Environment variable template for WriteSpace
#
# This project uses localStorage for all data persistence.
# No backend or external API keys are required.
#
# Optional branding variable (used via import.meta.env.VITE_APP_NAME)
VITE_APP_NAME=WriteSpace